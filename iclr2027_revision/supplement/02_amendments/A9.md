<!-- DERIVED SUPPLEMENTARY COPY. Not the contemporaneous original.
     Derived-copy creation date: 2026-08-29
     Canonical source: lock_amendment_A9.md
     Canonical source SHA-256: d55df29b2a3d3f40705d6811467b6ddc76c07ebdcd2f2aec8942b719adbd232e
     The canonical project artifact, not this copy, is the record of provenance. -->

# Lock Amendment A9 — Governance and Reporting Reconciliation

**Status: POST-UNBLINDING GOVERNANCE / REPORTING REPAIR.** This amendment is retrospective. It is
filed with the current revision, necessarily after external unblinding and after the primary
external result was persisted (`phase4_primary.json`, `persisted_at` 2026-08-13T06:15:35.490479+00:00).
It **alters no hypothesis, endpoint, split, control, seed, metric, held-out access rule, or
scientific decision rule.**

**Authored/recorded 2026-08-28.** The authoritative version-control filing date is the Git commit
timestamp of the commit that introduces this file; no Git date is backdated.

**Governing invariant.** Every scientific result value that A9 directs for immediate manuscript
reporting is drawn from already-persisted artifacts; where provenance is incomplete, A9 requires
repair or removal rather than treating the value as newly established.

**Governing repair principle.** Historical artifacts are preserved rather than silently repaired
retrospectively. Where an already-filed document contains inaccurate governance terminology, the
artifact is left unchanged, the correction is recorded here, and accurate terminology is used
prospectively. The sole exception is A8, which had not yet been added to version control and was
still being prepared for first filing.

---

## 1. Mandatory C2 reporting

`part1_scientific.md` §9, *Mandatory reporting*: "the C2 comparison and its interval, whatever it
shows." Lock §15 (H-FM1): "**Reported alongside, non-gating:** `ΔR² = R²(FM-only) − R²(C2)` with its
interval — the compressed-expression benchmark." The lock header states the specification governs
where the two could be read as differing.

Persisted values, VCC held-out target-gene test split:

    ΔR²(z − C2)  = +0.1107
    95% CI       = [+0.0906, +0.1317]

    R²(z)  = +0.1496
    R²(C2) = +0.0387
    R²(C1) = −0.0150

> **[Correction 2026-08-29 UTC]** An earlier version of this amendment, as committed at
> `1a00ae0ef22bd5eaac7dc544527b69f8350aa656`, recorded `sign-stability = 1.000` for `ΔR²(z − C2)`.
> The frozen Phase-1 record persists that statistic only for `ΔR²(z − C1)`
> (`phase1_vcc.md:261–264`); no sign-stability statistic was recorded for `z − C2`. The unsupported
> line has therefore been removed. The `z − C2` point estimate (+0.1107), its 95% interval
> ([+0.0906, +0.1317]), and every other scientific value and decision in this amendment are
> unchanged. The same factual correction was applied to the two corresponding statements in
> `main.tex`.

Persisted external values (`phase4/phase4_primary.json`, `screens[...].arm_rho`):

    C2, RPE1            rho ≈ −0.156
    C2, K562-essential  rho ≈ −0.264

C2 was evaluated during the already-authorised single VCC test evaluation
(`TEST_EVALUATIONS_PERFORMED = 1`); its predictions and results are persisted in
`phase1/preds/test_C2.npy`, `phase1/test_table.json`, `phase4/phase4_ext_predictions.npz` and
`phase4/phase4_primary.json`; the ΔR² and its interval were computed and recorded at the time in
`experiment_specs/phase1_vcc.md` under the same pre-registered inference. **Reporting C2 consumes no
new test access.** The omission from the manuscript was a reporting omission, not a missing analysis.

**Manuscript sites.** The in-distribution result paragraph and the H-FM1 outcome paragraph carry the
non-gating comparison; the transfer-results paragraph carries the persisted external values; and the
limitations passage carries one neutral clause noting that the pre-specified C2 comparator bears on
that question. A9 interprets no mechanism from C2 and authorises none.

## 2. Restoration of lock-era MAG-SPSM and SPSM reporting

Submission-era persisted values, re-derived from retained per-row prediction artifacts:

    MAG-SPSM   per-seed 0.222381, 0.229907, 0.223762  ->  mean +0.2253, SD (ddof=1) +0.0040
    SPSM       per-seed 0.076818, 0.093950, 0.076431  ->  mean +0.0824, SD (ddof=1) +0.0100

Later revision reproduction: MAG-SPSM mean ≈ +0.2251, SD ≈ +0.0168, displayed +0.225 ± 0.017;
SPSM displayed +0.077 ± 0.026.

- The lock-era values **re-derive exactly from retained per-row prediction artifacts**.
- The revision values **cannot be independently re-derived from equivalently retained per-row
  prediction artifacts currently preserved in the project**; they survive only through
  summary-level revision artifacts.
- **No amendment A1–A8 authorised the substitution.** It was directed by a decision record (§3).
- SPSM changed in both point estimate and dispersion; MAG-SPSM changed primarily in dispersion.

**Cause: UNRESOLVED.** The two runs agree on every recorded scientific setting examined — split
identity, row identities, label, feature source, HVG artifact, magnitude block, architecture,
epochs, batch size, learning rate, loss, optimiser, weight decay, gradient clipping, warmup, seeds
and the ddof convention. Nondeterministic GPU or environment-dependent behaviour is **a hypothesis
and not an established cause**, and A9 asserts no reproduction pattern in its support: at least one
classical arm's values were carried forward from an earlier gate run rather than independently
re-executed, so a "deterministic arms reproduced, nondeterministic arms did not" contrast cannot be
drawn cleanly from the surviving record. This preserves L15 Rule 5 (`UNRESOLVED`; do not speculate).

**Decision adopted:** restore `MAG-SPSM +0.225 ± 0.004` and `SPSM +0.082 ± 0.010`, with the
discrepancy disclosed in the reproducibility statement.

## 3. Supersession of L15 Rule 3, narrowly

`L15_author_decision.md` states, verbatim (lines 27–28):

> - The manuscript does **not** narrate the legacy-vs-revision discrepancy (Rule 3). This is
>   a new submission reporting its locked revision harness.

**A9 supersedes Rule 3 only to the extent that it operates as an instruction not to disclose a
now-established numerical provenance discrepancy.** Rules 1, 2, 4, 5 and 6 are untouched, including
Rule 5's UNRESOLVED finding.

## 4. Chronology terminology correction

**The status lines of A1 through A4 and A6 use the word "committed" in a sense the version-control
record does not support. Those documents are preserved unedited as historical artifacts; the
accurate characterisation and chronology are recorded here.** Leaving them unchanged does not make
the original wording correct. A7's status line does not carry this defect and is not modified.

### Verified Git facts

- **A1–A4** first entered version control in commit `5ca5e36` on **2026-08-25**.
- **A6–A7** first entered version control in commit `dfdff1e` on **2026-08-26**.
- **A8** was untracked before this revision's version-control filing.
- **A9** was created in this controlled revision.

In every tracked case the supported authoring or recording date precedes the first commit date, and
no amendment's stated date is corroborated by a Git commit on that date.

### Earliest supported existence

| amendment | earliest supported existence | evidence |
|---|---|---|
| A1 | 2026-08-11T19:09:21.979Z | contemporaneous machine-written session record, plus preserved editor file-history retaining the pre-edit content |
| A2 | 2026-08-13T03:16:13Z | ext4 crtime, corroborated by a session read ~3 min 14 s later |
| A3 | 2026-08-13T04:08:21Z | ext4 crtime, corroborated by a session read ~1 min 20 s later |
| A4 | 2026-08-13T05:37:46Z | ext4 crtime, corroborated by a session read ~57 s later |

**A1 nuance.** A1's substance is supported from **2026-08-11T19:09:21.979Z**; its **current bytes
reflect a recorded edit at 2026-08-13T00:59:34.920Z** that appended a §33 classification block
without altering substance. The pre-edit content is preserved in editor file history. Both facts
must be stated together.

**First positively located external-outcome access: 2026-08-13T06:15:09.591Z through
2026-08-13T06:15:35.490Z**, bounded below by the writing of the external prediction artifact (which
the executing script writes before opening the outcome) and above by the primary result's own
`persisted_at`. **Margin from A4: approximately 37 minutes 24 seconds.**

### What is NOT established

- No Git commit exists for A1–A4 on their stated dates.
- No external timestamp authority, no third-party timestamp, no cryptographic timestamp, no signed
  commit, and no immutable or append-only audit log.
- A **28.04-hour session-record coverage gap** exists spanning 2026-08-12
  (2026-08-11T20:36:10.673Z → 2026-08-13T00:38:35.106Z) across every session log on the machine.
  Session records **provide substantial but incomplete coverage of the experimental window**.
- The head-selection freeze falls inside that gap, supported instead by filesystem metadata and
  process-written evidence — `partC.log`, whose internal write order places the freeze record before
  every test entry, and whose logged elapsed time agrees with wall-clock to two seconds.
- **No positive `EXTERNAL_Y_READ = YES` runtime marker exists.**

### The defensible statement

> **A1–A4 precede the first positively located external-outcome access in the surviving local record.**

This must not be strengthened into a claim that the surviving record excludes every conceivable
earlier undocumented access — it does not, and the coverage gap falls before A2, A3 and A4 were
created. The evidence class is **STRONG_LOCAL_PROVENANCE**, resting on **distinct local evidence
mechanisms** whose independence from a single author-controlled machine is not established.

## 5. Correction of the internal pre-registration definition

The prior definition ("timestamped, hash-verified project specification with individually logged
amendments") exceeded the record on all three components: "timestamped" was unqualified; the
specification, lock and amendment chain carry no digest chain, only a single self-citation of the
specification's hash inside the lock; and no append-only per-amendment ledger existed.

The manuscript body now states only that "pre-registered" denotes an internal pre-analysis
specification and lock rather than registration in an external registry, and points to the
chronology-evidence appendix, which carries the local-provenance detail and its limitations.

## 6. Amendment-chain disclosure

| # | numbered event | canonical original | anonymised supplement copy |
|---|---|---|---|
| A1–A4 | yes | yes, created 2026-08-11 → 2026-08-13 (§4) | yes — created 2026-08-26, differing by anonymisation |
| **A5** | yes | **none — fileless** | **none** |
| A6, A7 | yes | yes | yes — byte-identical to the originals |
| A8 | yes | yes, created 2026-08-27 | added with this revision |
| A9 | yes | this document | added with this revision |

**The anonymised A1–A4 supplement copies are dated 2026-08-26 and are not contemporaneous with the
events they record.** A5 is a **fileless editorial/governance event**; A9 creates no A5 artifact and
does not resolve its filelessness.

### A8 — pre-filing metadata correction

A8 was authored and finalised on 2026-08-27 according to local records (birth 2026-08-27T19:16:29Z,
final mtime 2026-08-27T19:35:53Z; contemporaneous session records place it in use in that interval;
a supplementary archive built at approximately 2026-08-27T19:38:08Z contains a byte-identical
member). A8 was **not** Git-committed on 2026-08-27 and remained untracked until the filing
accompanying this revision. Because it had not yet been filed, its inaccurate status wording was
corrected before first filing, separating the authored/recorded date from the version-control
filing. A8's substantive findings are unchanged.

## 7. Package consistency

The manuscript states that the specification, its amendment chain, the environment record and the
artifact digests accompany the submission. The supplement is repaired to make that true: it now
carries `part1_scientific.md`, the experiment lock, A1–A4, A6, A7, A8, A9, the environment record,
the artifact digests, the README, and sanitized provenance derivatives. **A5 remains intentionally
absent as a fileless governance event.**

## 8. Mechanical and provenance repairs

### 8.1 Bibliography

The `replogle2022dataset` entry in `references.bib` had been truncated to two authors. The complete
author list is restored and `main.bbl` regenerated from the corrected source. This is attribution
repair only.

### 8.2 Bootstrap provenance digest

`pipeline/bootstrap_wiring.py` is unchanged and hash-matching. The recorded historical digest for
`scripts/bootstrap_ci/run_bootstrap.py` (`644dac6f…a50c794`) differs from the current digest
(`98556b76…5bc0040d`) as a consequence of commit `b1c4009`. The prior audit found that the diff
affected path resolution and import infrastructure and **did not change the statistical procedure**;
`B = 10,000`, `seed = 42` and the 2.5/97.5 percentile interval are unchanged. The documentation field
now records both digests. No recomputation was performed.

### 8.3 A8's "R10" citation

A8 cited a provenance audit "R10" that does not exist in the surviving project. A9 invents none. The
same verdict is supported by **A7 §§38–40** and **`R6_RESULTS.md` §B2**, and A8's citation is
repaired to those sources.

### 8.4 The counterfactual counts 37 and 29

**The previously reported 37/29 counterfactual construct counts were reconstructed under a frozen
metadata-only procedure from the pre-specified metadata/set definitions, after first reproducing the
historical 43/33 construct overlap counts. No model was run and no external outcome column was
accessed.**

The procedure was frozen before execution, executed exactly once, and exited zero:

    GLOBAL_HISTORICAL_VALIDATION = PASS
    RAW150 overlap      RPE1 43 constructs / 42 genes ; K562-essential 33 / 32
    ELIGIBLE140 overlap RPE1 37 constructs / 36 genes ; K562-essential 29 / 28

Result artifact: `provenance/37_29_reconstruction.json`.
Frozen script SHA-256: `9ed9deb1bd9123af4b136fd658c737f110f50102b9cca165e1e1ead548ee1ca9`.

The matching rule was recovered independently from the executed R6 Task-B code rather than chosen:
category strings as stored with bytes decoded where necessary, exact Python string equality, no
whitespace stripping, no case normalisation, no alias or synonym mapping, no Ensembl remapping; the
non-targeting literal `"non-targeting"` matched case-sensitively; constructs counted individually and
not de-duplicated by gene. This is a provenance reconstruction, **not proof and not scientific
validation**.

`reconstruct_nonoverlap_counts.py` and `37_29_PRE_RUN_FREEZE.json` are **not copied verbatim into the
public supplementary package**, because the canonical records embed machine-specific provenance and
path context. They remain part of the canonical project provenance and code-release record, and the
reconstruction is verifiable against the script SHA-256 cited above. Excluding them from the public
package does not make the evidence unavailable.

## 9. Chronology-evidence appendix

The manuscript carries an appendix stating the reconstructed ordering, its evidence classes and its
gaps: the earliest supported A1–A4 chronology; the first positively located external-outcome access
interval; the evidence classes (contemporaneous machine-written session records, filesystem creation
metadata, process-written run artifacts); the 28.04-hour session-record gap; the absence of external
timestamping or signature; the absence of a positive `EXTERNAL_Y_READ = YES` marker; and the frozen
37/29 reconstruction with its script digest. The local records are not described as immutable,
third-party, or independently attested.

## 10. Scope limits and invariants

A9 does **not**: create a new hypothesis; alter H-FM1 or any hypothesis; alter any gate, endpoint,
split, external screen, control definition, seed or test statistic; authorise new test access, new
held-out outcome access, model retraining, or recomputation of any scientific result; change the
already-observed external outcomes; or convert local provenance into external pre-registration.

A9 repairs **reporting**, **provenance wording**, **value provenance**, and **packaging consistency**.

- A9 does **not** edit A1–A4, A6 or A7; those remain unchanged historical artifacts.
- A9 does **not** resolve A5's filelessness.
- A9 does **not** resolve the MAG-SPSM / SPSM discrepancy cause.
- A9 does **not** assert that the record excludes every conceivable earlier undocumented
  external-outcome access.
- A9 does **not** claim that governance is proven, complete, or independently attested.
